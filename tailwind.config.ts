  const config = {
    theme: {
        extend: {
          colors: { primary: '#2563eb', muted: { DEFAULT: '#6b7280', foreground: '#9ca3af' } },
          fontFamily: { display: ['Pretendard','sans-serif'] },
        }
      },
      darkMode: 'class',
  };
  export default config;